#include "main.h"
/**
 * print_alphabet - print the alphabet in lowercase
 * Return: Always 0
 */

void print_alphabet(void)
{
	char z;

	for (z = 'a'; z <= 'z'; z++)
		_putchar(z);
	_putchar('\n');
}
