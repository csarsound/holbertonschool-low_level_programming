#include <stdio.h>
/**
 * main - designated start of the program
 * Return: Always 0
 */

int main(void)
{

	int clock;

	for (clock = 0; clock < 10; clock++)
	printf("%d", clock);
	putchar('\n');

	return (0);
}
