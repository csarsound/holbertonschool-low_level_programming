#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - designated start of the program
 * Return: Always 0
 */
int main(void)
{
	char e;

	for (e = 'a'; e <= 'z'; e++)
		putchar(e);
	putchar('\n');
		return (0);

}
