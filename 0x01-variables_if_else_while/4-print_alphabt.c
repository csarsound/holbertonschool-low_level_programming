#include <stdio.h>

/**
 * main - designated start of the program
 * Return: Always 0
 */
int main(void)
{
	char i;

	for (i = 97; i < 123; i++)
	{
		if (i == 101)
		{
		}
		else if (i == 113)
		{
		}
		else
		{
			putchar(i);
		}
	}
	putchar('\n');
	return (0);
}
